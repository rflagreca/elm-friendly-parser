import Parser exposing (Parser)
import Operator exposing (Operator, Rules, ParseResult)

parseResult : ParseResult o -> String
parseResult result =
     "TODO"

operator : Operator o -> String
operator op =
    "TODO"

rules : Rules o -> String
rules rules =
    "TODO"

parser : Parser o -> String
parser parser =
    "TODO"

-- fromString : String -> Parser o
-- fromString src =
--     "TODO"

-- encode : Parser o -> Json
-- encode parser =
--     "TODO"

-- decode : Json -> Parser o
-- decode json =
--     "TODO"
